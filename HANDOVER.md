# studios.ninja — מסירה (19.8.2026)

## מה עלה לאוויר
26 דפים, 22,995 מילים. היה: 6 דפים, 3,429 מילים.

| קבוצה | דפים |
|---|---|
| ראשי | `/` · `/about.html` · `/pricing.html` · `/services.html` |
| שירותים | `/shopify.html` · `/ecommerce.html` · `/cro.html` · `/migration.html` |
| עבודות | `/work/` + 6 case studies |
| מדריכים | `/blog/` + 5 מאמרים (1,400–1,720 מילים כל אחד) |
| משפטי | `/privacy.html` · `/accessibility.html` |
| פנימי (noindex) | `/thank-you.html` · `/questionnaire.html` · `/404.html` |

## הארכיטקטורה החדשה
```
assets/studios.css     מערכת עיצוב משותפת (29KB) — כל הדפים יורשים
assets/app.js          טרקינג + טופס + UI (22KB)
assets/analytics.js    GA4/GTM — ממתין למזהה
assets/media/*.webp    4 שכבות שמיים (228KB)
```
עמוד הבית ירד מ-207KB ל-63KB.

## ⚠️ פעולות שנשארו לך

### 1. מזהה GA4 או GTM
`assets/analytics.js` שורות 18-19. מלא אחד מהם והמדידה נדלקת בכל 26 הדפים:
```js
var GA4_ID = "";   // G-XXXXXXXXXX
var GTM_ID = "";   // GTM-XXXXXXX
```
כרגע ריק = לא נטען כלום. האירועים שכבר מחווטים: `generate_lead` · `form_start` · `contact_whatsapp` · `contact_phone` · `contact_email`.

### 2. למחוק לידי בדיקה מה-CRM
`lead_id 9b418b0a-e947-433e-939b-85e7a564c780` (טלפון 0500000001), ומבדיקת מסלול הליד ב-19.8: הטלפונים `0500000002`, `0500000009`, `0500000021`, `0500000031` — יש כמה שורות מחלקם.

### 3. תמונות צוות
`about.html` ועמוד הבית מכילים 4 כרטיסי צוות. אלון עם תמונה, ירין ושניים נוספים במצב ריק.
להוספה: תמונה ריבועית ≥600×600, WebP, בשם `team-<שם>.webp` בשורש. יש הערת HTML מעל `.team-grid` עם ההוראות המדויקות.

### 4. Search Console
להגיש מחדש את `sitemap.xml` (23 כתובות) ולבקש אינדוקס לדפים החדשים.

### 5. git
בוצע commit ב-19.8 (`58b11c1`) שכולל את 26 הדפים ואת עבודת הפגישות. **לא נדחף ל-remote.**

### 6. מיגרציה שממתינה ב-CRM
`supabase/migrations/20260819200000_landing_events_booking_click.sql` — מוסיפה `booking_click` ל-enum של `landing_events`. עד שהיא תרוץ ו-CRM ידפלוי, `/api/track` מחזיר 400 על קליקים ביומן (לא מזיק, נרשם בפיקסל בלבד). אחרי הדיפלוי זה נדלק לבד.

### 7. פער 15 מול 60 דקות
האתר מבטיח "שיחת ייעוץ חינם · 15 דקות". היומן מוגדר לפגישות של 60 דקות. מי שיקבע יראה את הפער.

## החלטות תוכן שקיבלתי בשמך — תעבור עליהן

| מה | למה |
|---|---|
| הוסר **"לא חל מע״מ"** מדף המחירים | חושף מעמד עוסק פטור. מול חברות גדולות זה מאותת על עסק קטן. הוחלף ב"מחיר סופי · בלי תוספות" |
| הוסרו **תנאי תשלום 50/50** מהאתר | תנאי מסחר הם החלטה שלך, לא תוכן אתר. הוחלף ב"פריסה נקבעת יחד ומופיעה בהצעה" |
| **"3-4 סבבי שינויים"** → "היקף מוגדר בהצעה" | האתר החי שלך הבטיח 3-4 סבבים, ההצעות שלך אומרות סבב אחד. חשיפה מסחרית אמיתית |
| אפס ציטוטי לקוחות | לא ממציאים המלצות. נשארו 7 סרטוני היוטיוב האמיתיים |
| אפס מספרי ביצוע של לקוח | לפי המדיניות שלך. רק 47 / ₪2.4M / 3.2% המצטברים |

## מה נוסף ב-19.8 (אחרי המסירה)
- **מסלול פגישה ישיר בכל 25 הדפים** — פוטר, תפריט מובייל, באנרי CTA בדפי השירות, בלוק מתחת לטופס, וכפתור במסך סיכום השאלון (שגם יוצר ליד ב-CRM).
- **תוקן:** `leads_user_phone_uniq` בסופאבייס חסם כל טלפון שכבר היה ב-CRM — הטופס החזיר 500 ו"השליחה נכשלה". האינדקס הוסר.
- **תוקן:** השאלון שלח ל-Formspree כל 30 שניות ושרף את מכסת ה-50/חודש. עכשיו תקרה של 2 שמירות חלקיות למבקר.

## חזרה לאחור
```bash
netlify rollback --site 85b36c75-f97f-44fd-8eea-504506a263d7
```
או דרך Netlify → Deploys → הגרסה הקודמת → Publish deploy.
גיבוי מקומי של עמוד הבית הישן: `_backup-index-20260819.html`

## כלים שנשארו לך
```bash
python3 /tmp/qa.py          # בדיקת תקינות כל הדפים
python3 /tmp/sitemap.py     # בנייה מחדש של sitemap
python3 /tmp/sync_faq.py <file>   # סנכרון סכימת FAQ לטקסט הגלוי
```
(מומלץ להעתיק אותם לתוך הרפו — הם ב-/tmp וימחקו)
